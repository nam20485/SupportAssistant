{
	"name": "Support Assistant devcontainer",
	"image": "ghcr.io/nam20485/dotnet-prebuild:latest",
	// Ensure the image includes the .NET 10 SDK (see global.json).
	// "postCreateCommand": "dotnet restore SupportAssistant.sln",
	"remoteUser": "root"
}
